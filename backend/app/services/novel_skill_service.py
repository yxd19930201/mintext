"""Runtime adapter for the bundled novel-continuity-writer skill.

The original skill contains the full workflow and reference material.  This
module keeps API prompts small by injecting only the rules needed at each AI
generation phase.
"""
from __future__ import annotations


_BASE_RULES = """你正在遵循 novel-continuity-writer 长篇小说连续性创作规范。
- 把故事当作持续运行的内容产品；聊天内容不能覆盖已提供的小说正史、设定和连续性记忆。
- 大故事提供长期目标，小故事提供即时追读；每章至少推进冲突、信息、资源、关系、名声或威胁之一。
- 主角必须主动选择；阻力方必须有资源、位置或合理判断，不能靠降智送机会。
- 压力、选择、反馈、收益与新代价要形成因果链；章末必须留下危险、选择、期限、收益或揭露。
- 人物有独立欲望与不同口吻；用现场动作和反馈替代解释腔、总结腔与模板化枚举。
- 不得改变上下文中的姓名、身份、伤势、物件归属、知识差、时间顺序、承诺和已发生事件。
- 每个角色的语言能力、常用语言、口吻、口头禅及对他人的称呼属于连续性状态；不得无依据改变。
- 称呼必须按“说话者→被称呼者”分别记录。关系升级、结婚、决裂或身份变化后可以改变称呼，但必须由正文事件触发并记录生效章节，不能提前使用。
- 不会某种语言或方言的角色不得突然使用；临时翻译、模仿或学习必须有明确场景依据。
"""

_PHASE_RULES = {
    "roadmap": """当前任务是把故事梗概转换为全书固定阶段路线图。
- 必须识别梗概中按时间或因果排列的全部重大阶段、行业、目标和结局承诺，并覆盖到总章节数。
- 每个阶段必须有连续且不重叠的章节范围、进入条件、退出条件、阶段成果、主要风险和通向下一阶段的过渡事件。
- 后一阶段必须继承前一阶段已经获得或失去的身份、财富、资产、能力、关系和名声，不得把主角状态无故清零。
- 为主要角色建立 dialogue_profiles：记录languages、forbidden_languages、default_register、speech_habits，以及对其他角色的默认称呼。
- 建立 relationship_states：按角色对记录当前关系、双方称呼、生效章节和变化条件。
- 只输出调用方要求的 JSON。""",
    "outline": """当前任务是规划章节。
- 全书默认锁定同一位核心主角、同一条主线和同一套世界规则；除非项目设定明确要求群像或双主角，否则禁止中途更换主角。
- 分批生成时，已提供的前置章节属于不可改写的正史；新批次必须直接承接上一章的结果与钩子，不得因批次切换重新开局、重设身份或另起无关剧情。
- 新人物必须通过既有因果进入，只能承担明确的配角、对手、盟友或阶段功能，不得无铺垫夺取主角位置。
- 每3至5章形成一个可局部结算的小故事，同时服务全书主线。
- 每章简介必须明确写出：冲突、推进、即时满足/反馈、章尾钩子，以及产生的不可逆变化。
- 相邻章节的因果、时间、人物目标和伏笔必须连续；避免连续重复同类爽点或互动。
- 每章必须给出 speech_constraints，明确本章主要人物使用的语言、口吻及相互称呼。
- 只有本章确实发生关系升级、结婚、决裂、职位变化等事件时，才能填写 relationship_changes 和 address_changes；变化必须包含双方、旧值、新值、原因及生效章节。
- 只输出调用方要求的 JSON，不要附加说明。""",
    "audit_outline": """当前任务是审核候选章节大纲，未通过时给出可直接替换的修订稿。
- 对照固定阶段路线图、已有正史大纲、人物状态账本和不可逆事实逐项检查。
- 检查主角、身份、财富、资产、能力、关系、时间线、阶段范围、进入/退出条件及自然过渡。
- 已获得的状态不得无原因消失；若发生降级，必须存在足够强、可定位且符合因果的损失事件。
- 逐项检查角色语言能力、默认口吻和“说话者→被称呼者”称呼是否符合账本；关系变化前不得提前使用新称呼。
- 审核通过只能在没有硬冲突时给出；不通过时必须列出问题并返回完整修订章节。
- 只输出调用方要求的 JSON。""",
    "draft": """当前任务是撰写章节正文。
- 严格兑现本章标题和简介，不越写到后续章节，也不提前回收未安排的长线伏笔。
- 从明确压力进入场景，让人物通过动作、对白和选择推动冲突；至少出现一次反馈或局部结算。
- 结尾令信息、资源、关系、风险或位置发生变化，并自然制造下一章追读动力。
- 删除可移除而不影响主线、人物或信息的水段；避免“他知道/他意识到”、抽象震惊和作者总结。
- 本章若改变主角现金，必须逐笔写清主角本人实际到账收入、实际支付费用和期末余额，且“期初＋收入－支出＝期末”；禁止用“此前零散收入、其他进账、若干收入”等未列金额填补差额。
- 公司、工厂、贸易部或客户账户的货款、定金和采购支出必须与主角个人现金分开；正文明确由公司账户结算或主角未垫付时，不得再计入主角收支。
- 输出只有小说正文，不写分析、提纲、创作说明或 Markdown 围栏。""",
    "audit_draft": """当前任务是审核候选小说正文是否能进入正史。
- 对照本章大纲、阶段路线图、上一章结尾、人物状态账本和不可逆事实检查全部硬冲突。
- 核对财富资产、身份职业、能力、关系、时间地点、伤势、物品归属、知识边界、承诺和未决线索。
- 已获得的状态不能无原因下降或消失；合理受挫必须有正文内明确因果、损失规模和后续状态。
- 同时检查正文是否完成本章目标、承接上一章并产生可回写的新变化。
- 逐句检查每段对白的说话者、语言/方言、口吻和称呼。旁白写人物姓名不等于对白称呼变化。
- 若正文出现新称呼或语言能力，必须能定位到明确的关系变化、学习或身份变化事件，否则判定不通过。
- 若出现主角现金变化，逐笔复算主角本人收入和支出；未列金额的“此前/其他/零散收入”不得用于凑平余额，公司账户收支不得混入个人流水。
- 有任一硬冲突必须判定不通过；只输出调用方要求的 JSON。""",
    "canon": """当前任务是在正文审核通过后更新数据库正史账本。
- 以已有状态账本和不可逆事实为基线，只根据已通过审核的本章正文更新。
- 返回完整最新状态账本，并单独列出本章新增的不可逆事实；不得把推测或未来大纲写成既成事实。
- 财富、资产、债务必须说明变化前后与原因；身份、关系、能力、物品、伤势、知识和承诺同理。
- transaction_ledger只记录主角本人现金和资产变化。公司、工厂、贸易部或客户账户结算的货款、定金、采购款不得改变主角cash_change；若正文明确“个人未垫付/不经手/分文未沾”，该业务记录的personal_cash_effect必须为false且cash_change为0。
- 完整保留 dialogue_profiles 和 relationship_states。仅当正文明确发生关系、婚姻、身份或语言学习事件时，才能追加带生效章节与原因的称呼/语言版本；禁止用本章偶然一句对白覆盖长期规则。
- 只输出调用方要求的 JSON。""",
    "next": """当前任务是设计紧接正史的下一章。
- 从上一章尚未解决的压力、收益或代价中选择最强发动机。
- 简介必须给出具体诱因、阻力、主角选择、局部结算和新钩子，并保持人物知识与物件持有链连续。
- 只输出调用方要求的 JSON，不要附加说明。""",
    "memory": """当前任务是更新正史连续性记忆。
- 完整保留仍有效的既有事实，只根据本章正文追加或更新；不得把推测写成事实。
- 记录人物状态/关系、关键事件、未回收线索，以及时间地点、资源物件、伤势、知识状态和承诺的变化。
- 关系描述要短，但关键事件和未决线索必须足以驱动后续章节。
- 只输出调用方要求的 JSON，不要附加说明。""",
}


def novel_skill_prompt(phase: str, custom_prompt: str | None = None) -> str:
    """Compose the mandatory skill prompt with an optional user prompt."""
    phase_rules = _PHASE_RULES.get(phase)
    if phase_rules is None:
        raise ValueError(f"Unknown novel skill phase: {phase}")
    parts = [_BASE_RULES.strip(), phase_rules.strip()]
    if custom_prompt and custom_prompt.strip():
        parts.append("项目自定义文风与要求（不得违反以上连续性规则）：\n" + custom_prompt.strip())
    return "\n\n".join(parts)
